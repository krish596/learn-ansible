- name: Demo On Templates
  hosts: localhost
  connection: local

  vars:
    fruit_name: apple
  tasks:

    - name: Copied template file
      ansible.builtin.template:
        src: test.yml
        dest: /tmp/test.yml
